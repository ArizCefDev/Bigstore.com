@model IEnumerable<DTO.EntityDTO.AboutDTO>
@{
    ViewData["Title"] = "AboutIndex";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div id="page-wrapper">
    <div class="main-page">
        <div class="tables">
            <h2 class="title1">Haqqımızda</h2>
            <div class="table-responsive bs-example widget-shadow">
                <h4><a href="/Admin/AboutAdd/" class="btn btn-primary"><i class="fa fa-plus"></i></a></h4>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Mətn</th>
                            <th>Foto 1</th>
                            <th>Foto 2</th>
                            <th>Sil</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <th scope="row">@item.ID</th>
                                <td>@item.Text</td>
                                <td><img src="@item.ImageURL1" style="width:100px; height:100px;" /></td>
                                <td><img src="@item.ImageURL2" style="width:100px; height:100px;" /></td>
                                <td>
                                    <a href="/Admin/AboutDelete/@item.ID/" class="btn btn-danger"><i class="fa fa-trash"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

