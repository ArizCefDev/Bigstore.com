@model IEnumerable<DTO.EntityDTO.MessageDTO>

@{
    ViewData["Title"] = "MessagesIndex";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div id="page-wrapper">
    <div class="main-page">
        <div class="tables">
            <h2 class="title1">Mesajlar</h2>
            <div class="table-responsive bs-example widget-shadow">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Adı</th>
                            <th>Gmaili</th>
                            <th>Mesajı</th>
                            <th>Tarix</th>
                            <th>Sil</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <th scope="row">@item.ID</th>
                                <td>@item.Name</td>
                                <td>@item.Email</td>
                                <td>@item.Text</td>
                                <td>@item.CreateDate</td>
                               
                                <td>
                                    <a href="/Admin/MessagesDelete/@item.ID/" class="btn btn-danger"><i class="fa fa-trash"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

